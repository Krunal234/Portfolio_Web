const IntroStrip = () => {
  return (
    <section className="section intro-strip" aria-label="Positioning" data-reveal>
      <p>
        I work close to the engine room: secure REST APIs, EF Core interceptors, Outbox Pattern,
        Kafka event delivery, Redis-backed validation, multi-database provider abstractions,
        Azure serverless workflows, queue optimization, and the reliability details that keep
        production systems calm under pressure.
      </p>
    </section>
  );
};

export default IntroStrip;
